namespace MeshSimplificationComparer
{
    public enum Ext
    {
        txt, // Plain Text
        obj, // OBJ file format (mesh)
        stl, // STL file format (mesh)
        mlx, // MeshLab Script file format
        mlp, // MeshLab Project file format
        tex, // LaTeX
        csv,
        ofs, // Open Flipper Script
        off, // Object File Format (mesh)
        png,  // Portable Network Graphics
        ply //Polygon File Format or the Stanford Triangle Format
    }
}
